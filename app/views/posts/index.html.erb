<li>
	<h3>Результаты поиска по запросу <em><%= params[:term]%></em></h3>
  <% @posts.each do |post| %> 
  <% if post.respond_to?(:pg_search_highlight) %>
    <%= post.pg_search_highlight.html_safe %>
  <% else %>
    <%= post.title %>
    <hr>
     <%= post.content %>
  <% end %>
  <% end %>
</li>
